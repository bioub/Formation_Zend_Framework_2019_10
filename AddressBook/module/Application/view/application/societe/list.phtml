<?php $this->headTitle('Liste des sociétés') ?>

<h2>Liste des sociétés</h2>

<ul>
    <?php foreach ($societes as $s) : ?>
    <li>
        <a href="<?= $this->url('societes/show', ['id' => $this->escapeUrl($s->getId()) ]) ?>">
            <?= $this->escapeHtml($s->getNom()) ?>
        </a>
    </li>
    <?php endforeach; ?>
</ul>